<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core">
    <Dialog id="createUserDialog" title="Create User">
        <VBox class="sapUiSmallMargin">
            <Label text="Name"/>
            <Input id="inputName11" value="{createUserModel>/firstName}" />
            
            <Label text="Email"/>
            <Input id="inputEmail22" value="{createUserModel>/email}" />
            
            <Label text="Contact"/>
            <Input id="inputContact3" value="{createUserModel>/contact}" />

            <Label text="Created-By"/>
            <Input id="inputContact4" value="{createUserModel>/createdBy}" />

            <Label text="Updated-By"/>
            <Input id="inputContact5" value="{createUserModel>/updatedBy}" />

            <Label text="Current-Address"/>
            <Input id="inputContact6" value="{createUserModel>/currentAddress}" />

            <Label text="Current-Address"/>
            <Input id="inputContact7" value="{createUserModel>/permanentAddress}" />

            <Label text="Password"/>
            <Input id="inputContact8" value="{createUserModel>/password}" />
            
            <!-- <Label text="Gender"/>
            <Select id="inputGender44" selectedKey="{createUserModel>/gender}">
                <items>
                    <core:Item key="Male" text="Male"/>
                    <core:Item key="Female" text="Female"/>
                    <core:Item key="Other" text="Other"/>
                </items>
            </Select> -->
        </VBox>
        <beginButton>
            <Button text="Save" press=".onSaveCreateUser"/>
        </beginButton>
        <endButton>
            <Button text="Cancel" press=".onCloseDialog"/>
        </endButton>
    </Dialog>
</core:FragmentDefinition>
